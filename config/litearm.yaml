# litearm_ros1 bridge configuration
# (merged on top of the launch-file args)
endpoint: tcp/127.0.0.1:7447
arm_id: armA
loop_hz: 50.0
cmd_speed: 0.5
joint_names: ["joint0", "joint1", "joint2", "joint3", "joint4", "joint5", "joint6"]
base_frame: base_link
tcp_frame: tool0
